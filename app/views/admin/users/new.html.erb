<div class="form_title" style="text-align:center;">
  <h2>Создание администратора</h2>
</div>
<%= form_for @admin, :url => users_path do |f| %>
    <%= render 'form', :f => f %>
    <%= f.submit 'Создать' %>
    <%= link_to 'Отменить', users_path %>
<% end %>