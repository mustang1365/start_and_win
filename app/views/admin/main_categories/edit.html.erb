<h1>Редактирование категории</h1>
<%= simple_nested_form_for @main_category, :url => url_for(:action => :update, :controller => :main_categories, :namespace => :admin, :method => :post) do |f| %>
    <%= render 'form', :f => f %>
<% end %>
